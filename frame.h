#pragma once

// By making this frame in its own file, any file can just #include "frame.h" and start messing with the frame (reading/writing to it) without having to prop drill it into nested functions like those in collision_detection
extern long frame;

